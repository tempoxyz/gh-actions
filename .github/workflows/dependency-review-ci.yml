name: Dependency Review

on:
  pull_request:

permissions: {}

jobs:
  dependency-review:
    name: Dependency Review
    uses: ./.github/workflows/dependency-review.yml # zizmor: ignore[self-repository] actionlint 1.7.12 does not support $/ references yet.
    permissions:
      contents: read
      id-token: write
      pull-requests: write
