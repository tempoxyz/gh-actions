name: Dependency Scan

on:
  pull_request:
  merge_group:

permissions: {}

jobs:
  scan:
    name: Dependency Scan
    uses: ./.github/workflows/dependency-scan.yml # zizmor: ignore[self-repository] actionlint 1.7.12 does not support $/ references yet.
    permissions:
      contents: read
      id-token: write
