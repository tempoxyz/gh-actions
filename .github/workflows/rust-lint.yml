name: rust lint

on:
  workflow_call:
    inputs:
      run-clippy:
        description: Run cargo clippy.
        required: false
        type: boolean
        default: true
      run-fmt:
        description: Run cargo fmt.
        required: false
        type: boolean
        default: true
      run-typos:
        description: Run typos.
        required: false
        type: boolean
        default: true
      run-deny:
        description: Run cargo deny.
        required: false
        type: boolean
        default: true
      clippy-runner:
        description: Runner used for cargo clippy.
        required: false
        type: string
        default: depot-ubuntu-latest-4
      fmt-runner:
        description: Runner used for cargo fmt.
        required: false
        type: string
        default: depot-ubuntu-latest
      typos-runner:
        description: Runner used for typos.
        required: false
        type: string
        default: depot-ubuntu-latest
      deny-runner:
        description: Runner used for cargo deny.
        required: false
        type: string
        default: ubuntu-latest
      timeout-minutes:
        description: Timeout for each lint job.
        required: false
        type: number
        default: 30
      checkout-submodules:
        description: Submodule checkout mode passed to actions/checkout.
        required: false
        type: string
        default: "false"
      working-directory:
        description: Repository-relative directory containing Cargo.toml and the files to lint.
        required: false
        type: string
        default: .
      rust-toolchain:
        description: Rust toolchain used for clippy and fmt.
        required: false
        type: string
        default: nightly
      deny-rust-toolchain:
        description: Rust toolchain installed on the deny runner and used inside the cargo-deny container.
        required: false
        type: string
        default: stable
      clippy-flags:
        description: Flags passed to cargo clippy.
        required: false
        type: string
        default: --all-targets --all-features --locked
      fmt-flags:
        description: Flags passed to cargo fmt.
        required: false
        type: string
        default: --all --check
      deny-flags:
        description: Additional flags passed to cargo deny check all.
        required: false
        type: string
        default: --all-features
permissions: {}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full

jobs:
  clippy:
    name: clippy
    if: ${{ inputs.run-clippy }}
    runs-on: ${{ inputs.clippy-runner }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    env:
      RUSTC_WRAPPER: sccache
    permissions:
      contents: read
    steps:
      # PR-controlled tools must not share a job with OIDC or STS credentials.
      - name: Harden the runner without policy-store credentials
        uses: tempoxyz/gh-actions/vendor/step-security/harden-runner@3a9189ec4c3d19f2863398822c73edd06b33fba3
        with:
          egress-policy: block
          # Checkout, mold/sccache releases, Rust toolchains, and Cargo dependencies.
          allowed-endpoints: >-
            github.com:443
            api.github.com:443
            codeload.github.com:443
            release-assets.githubusercontent.com:443
            static.rust-lang.org:443
            index.crates.io:443
            static.crates.io:443
          use-policy-store: false
          api-key: ""
          policy: ""
          token: ""

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: ${{ inputs.checkout-submodules }}
      - name: Install Rust toolchain
        env:
          RUST_TOOLCHAIN: ${{ inputs.rust-toolchain }}
        run: |
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal --component clippy
          rustup default "$RUST_TOOLCHAIN"
      - name: Install mold linker
        env:
          MOLD_VERSION: "2.42.0"
          MOLD_SHA256_X86_64: f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d
          MOLD_SHA256_AARCH64: 3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190
        run: |
          set -euo pipefail
          # mold publishes no checksums, signatures or attestations, so the release tarball digests
          # are pinned here and change together with MOLD_VERSION (test.yml keeps this block in
          # step with the copy in .github/workflows/rust-build-binaries.yml).
          ARCH="$(uname -m)"
          case "$ARCH" in
            x86_64)  EXPECTED="$MOLD_SHA256_X86_64" ;;
            aarch64) EXPECTED="$MOLD_SHA256_AARCH64" ;;
            *) echo "::error::mold: unsupported architecture $ARCH"; exit 1 ;;
          esac
          ASSET="mold-${MOLD_VERSION}-${ARCH}-linux.tar.gz"
          TMP="${RUNNER_TEMP:?}/mold"
          mkdir -p "$TMP"
          curl -fsSL --retry 3 -o "$TMP/$ASSET" "https://github.com/rui314/mold/releases/download/v${MOLD_VERSION}/${ASSET}"
          echo "$EXPECTED  $TMP/$ASSET" | sha256sum -c
          SUDO=""
          [ "$(id -u)" = 0 ] || SUDO=sudo
          $SUDO tar -C /usr/local --strip-components=1 --no-overwrite-dir -xzf "$TMP/$ASSET"
          # Make mold the default linker, as rui314/setup-mold did.
          if [ "$(realpath /usr/bin/ld)" != /usr/local/bin/mold ]; then
            $SUDO ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld)"
          fi
          mold --version
      - name: Enable sccache
        uses: tempoxyz/gh-actions/vendor/mozilla-actions/sccache-action@6f436eb3a168279692739e8c0c9fdb52b94bfae7
      - name: Run clippy
        working-directory: ${{ inputs.working-directory }}
        run: |
          # CLIPPY_FLAGS is a space-separated flag list that must word-split.
          # shellcheck disable=SC2086
          cargo clippy $CLIPPY_FLAGS
        env:
          CLIPPY_FLAGS: ${{ inputs.clippy-flags }}
          RUSTFLAGS: -D warnings

  fmt:
    name: fmt
    if: ${{ inputs.run-fmt }}
    runs-on: ${{ inputs.fmt-runner }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    permissions:
      contents: read
    steps:
      # PR-controlled tools must not share a job with OIDC or STS credentials.
      - name: Harden the runner without policy-store credentials
        uses: tempoxyz/gh-actions/vendor/step-security/harden-runner@3a9189ec4c3d19f2863398822c73edd06b33fba3
        with:
          egress-policy: block
          # Checkout and rustfmt toolchain installation; no crate downloads needed.
          allowed-endpoints: >-
            github.com:443
            api.github.com:443
            codeload.github.com:443
            static.rust-lang.org:443
          use-policy-store: false
          api-key: ""
          policy: ""
          token: ""

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        env:
          RUST_TOOLCHAIN: ${{ inputs.rust-toolchain }}
        run: |
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal --component rustfmt
          rustup default "$RUST_TOOLCHAIN"
      - name: Run fmt
        working-directory: ${{ inputs.working-directory }}
        run: |
          # FMT_FLAGS is a space-separated flag list that must word-split.
          # shellcheck disable=SC2086
          cargo fmt $FMT_FLAGS
        env:
          FMT_FLAGS: ${{ inputs.fmt-flags }}

  typos:
    name: typos
    if: ${{ inputs.run-typos }}
    runs-on: ${{ inputs.typos-runner }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    permissions:
      contents: read
    steps:
      # PR-controlled tools must not share a job with OIDC or STS credentials.
      - name: Harden the runner without policy-store credentials
        uses: tempoxyz/gh-actions/vendor/step-security/harden-runner@3a9189ec4c3d19f2863398822c73edd06b33fba3
        with:
          egress-policy: block
          # Checkout, typos release download, and GitHub release verification.
          allowed-endpoints: >-
            github.com:443
            api.github.com:443
            codeload.github.com:443
            release-assets.githubusercontent.com:443
          use-policy-store: false
          api-key: ""
          policy: ""
          token: ""

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      # typos publishes no build provenance, but its releases are immutable and GitHub attests
      # every asset, so `gh release verify-asset` proves the tarball is the one attached to the
      # release. Same target, annotations and exit status as the crate-ci/typos action.
      - name: Install typos
        env:
          TYPOS_VERSION: v1.49.0
          GH_TOKEN: ${{ github.token }}
        run: | # zizmor: ignore[github-env] INSTALL_DIR is created under RUNNER_TEMP, not derived from user input.
          set -euo pipefail
          case "$(uname -m)" in
            x86_64|aarch64) ARCH="$(uname -m)" ;;
            *) echo "::error::typos: unsupported architecture $(uname -m)"; exit 1 ;;
          esac
          ASSET="typos-${TYPOS_VERSION}-${ARCH}-unknown-linux-musl.tar.gz"
          INSTALL_DIR="${RUNNER_TEMP:?}/typos-bin"
          mkdir -p "$INSTALL_DIR"
          curl -fsSL --retry 3 -o "$INSTALL_DIR/$ASSET" "https://github.com/crate-ci/typos/releases/download/${TYPOS_VERSION}/${ASSET}"
          gh release verify-asset "$TYPOS_VERSION" "$INSTALL_DIR/$ASSET" --repo crate-ci/typos
          tar -xzf "$INSTALL_DIR/$ASSET" -C "$INSTALL_DIR" ./typos
          echo "$INSTALL_DIR" >> "$GITHUB_PATH"
      - name: Check for typos
        working-directory: ${{ inputs.working-directory }}
        run: |
          set -euo pipefail
          typos . --format json \
            | jq --raw-output 'select(.type == "typo") | "::warning file=\(.path | ltrimstr("./")),line=\(.line_num),col=\(.byte_offset + 1)::\"\(.typo)\" should be \"" + ((.corrections // []) | join("\" or \"")) + "\"."' \
            || true
          typos .

  deny:
    name: cargo deny check
    if: ${{ inputs.run-deny }}
    runs-on: ${{ inputs.deny-runner }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    permissions:
      contents: read
    steps:
      # PR-controlled tools must not share a job with OIDC or STS credentials.
      - name: Harden the runner without policy-store credentials
        uses: tempoxyz/gh-actions/vendor/step-security/harden-runner@3a9189ec4c3d19f2863398822c73edd06b33fba3
        with:
          egress-policy: block
          # Checkout, RustSec/git dependencies, Rust toolchains, and crates.io.
          allowed-endpoints: >-
            github.com:443
            api.github.com:443
            codeload.github.com:443
            static.rust-lang.org:443
            index.crates.io:443
            static.crates.io:443
          use-policy-store: false
          api-key: ""
          policy: ""
          token: ""

      - name: Verify OIDC request credentials are unavailable
        run: |
          if [ -n "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" ] || [ -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" ]; then
            echo "::error::Rust lint must not receive OIDC request credentials"
            exit 1
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        env:
          RUST_TOOLCHAIN: ${{ inputs.deny-rust-toolchain }}
        run: |
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal
          rustup default "$RUST_TOOLCHAIN"
      - uses: tempoxyz/gh-actions/vendor/EmbarkStudios/cargo-deny-action@6f436eb3a168279692739e8c0c9fdb52b94bfae7
        with:
          manifest-path: ${{ inputs.working-directory }}/Cargo.toml
          rust-version: ${{ inputs.deny-rust-toolchain }}
          command: check all
          arguments: ${{ inputs.deny-flags }}

  lint-success:
    name: lint success
    runs-on: ubuntu-latest
    permissions: {}
    if: always()
    needs:
      - clippy
      - fmt
      - typos
      - deny
    timeout-minutes: ${{ inputs.timeout-minutes }}
    steps:
      # PR-controlled tools must not share a job with OIDC or STS credentials.
      - name: Harden the runner without policy-store credentials
        uses: tempoxyz/gh-actions/vendor/step-security/harden-runner@3a9189ec4c3d19f2863398822c73edd06b33fba3
        with:
          egress-policy: block
          # No application downloads. A nonempty list avoids an empty-policy fallback.
          allowed-endpoints: api.github.com:443
          use-policy-store: false
          api-key: ""
          policy: ""
          token: ""

      - name: Verify OIDC request credentials are unavailable
        run: |
          if [ -n "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" ] || [ -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" ]; then
            echo "::error::Rust lint must not receive OIDC request credentials"
            exit 1
          fi

      - name: Decide whether the needed jobs succeeded or failed
        uses: tempoxyz/gh-actions/actions/check-needs@ab45d76f6e7420a094281a22aee1973ecc0754f8
        with:
          jobs: ${{ toJSON(needs) }}
          allowed-skips: >-
            ${{ !inputs.run-clippy && 'clippy' || '' }}
            ${{ !inputs.run-fmt && 'fmt' || '' }}
            ${{ !inputs.run-typos && 'typos' || '' }}
            ${{ !inputs.run-deny && 'deny' || '' }}
