name: rust deny

on:
  workflow_call:
    inputs:
      rust-toolchain:
        description: Rust toolchain installed on the runner and used inside the cargo-deny container.
        required: false
        type: string
        default: stable
      flags:
        description: Additional flags passed to cargo deny check all.
        required: false
        type: string
        default: --all-features
      runner:
        description: Runner used for cargo deny.
        required: false
        type: string
        default: ubuntu-latest
      timeout-minutes:
        description: Timeout for each job.
        required: false
        type: number
        default: 30

permissions: {}

jobs:
  deny:
    # Call the reusable workflow at the published, pinned commit.
    uses: tempoxyz/gh-actions/.github/workflows/rust-lint.yml@7e2ea6e1a8f5a22f16c2d4b389ffde25da93fa1b # 2026-09-22T22-21-59Z-7e2ea6e1
    permissions:
      contents: read
      id-token: write
    with:
      run-clippy: false
      run-fmt: false
      run-typos: false
      run-deny: true
      deny-rust-toolchain: ${{ inputs.rust-toolchain }}
      deny-flags: ${{ inputs.flags }}
      deny-runner: ${{ inputs.runner }}
      timeout-minutes: ${{ inputs.timeout-minutes }}
