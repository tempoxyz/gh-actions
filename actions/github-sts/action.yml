---
name: "GitHub STS"
description: >-
  Exchange the workflow OIDC token for a short-lived GitHub App token

inputs:
  scope:
    description: >-
      Repository (org/repo) or organization whose trust policy to use
    required: false
    default: ""
  policy:
    description: "Trust policy name"
    required: true
  dev:
    description: "(Dev testing only) Use the development STS endpoint"
    required: false
    default: "false"

outputs:
  token:
    description: "Short-lived GitHub App installation token"
  expires-at:
    description: "Token expiration timestamp"

runs:
  using: "node24"
  main: "main.js"
  post: "post.js"
