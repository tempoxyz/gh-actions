# yaml-language-server: $schema=https://raw.githubusercontent.com/suzuki-shunsuke/pinact/refs/heads/main/json-schema/pinact.json
version: 3
min_age:
  value: 7
  always: true
rules:
  # First-party Socket STS revisions are consumed by the wrapper immediately
  # after review and remain pinned by SHA, so they cannot satisfy the soak period.
  - ignore: true
    conditions:
      - expr: ActionName == "tempoxyz/gh-actions/actions/socket-firewall/sts"
  # The shim-capable Socket action revision is not tagged yet. Keep its
  # immutable pin while exempting only this action from comment/min-age checks.
  - ignore: true
    conditions:
      - expr: ActionName == "SocketDev/action"
  - ignore: true
    conditions:
      - expr: ActionName == "dtolnay/rust-toolchain"
  - ignore: true
    conditions:
      - expr: |
          ActionName matches "^docker://.+" && ActionVersion matches "^sha256:[0-9a-f]{64}$"
